export interface HeaderConfig {
  label: string;
  title: string;
  subtitle: string;
}

export interface ReportConfig {
  header: HeaderConfig;
  tocTitle: string;
  authors: string;
  university: string;
}
